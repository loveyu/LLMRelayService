# 2026-08-17

### 修复

- Rust Proxy 增加 `/api/hello` 及 `/openai/api/hello`、`/anthropic/api/hello` 兼容性健康探测端点，复用 `/health` 状态响应，避免客户端使用 `HEAD {base}/api/hello` 检查 Base URL 时误入模型路由并产生 `No route found` 告警。
