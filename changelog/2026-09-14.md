# 2026-09-14

### 修复

- Rust 代理在等待上游响应前即写入请求日志，避免非流式慢请求期间客户端超时取消后整条日志丢失。
- 请求日志新增断开来源、断开时间与实际耗时：客户端中止记为 `499/client`，上游连接或响应体中断记为 `502/upstream`，Console 列表与详情会显式区分并展示断开时刻。
- Console 请求日志的对话视图现在会实际渲染 OpenAI Chat Completions、Responses API 与 Anthropic payload 中的图片内容，不再只显示“图片”占位文字。
